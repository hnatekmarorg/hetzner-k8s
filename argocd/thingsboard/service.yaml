# apiVersion: v1
# kind: Service
# metadata:
  # name: thingsboard
  # namespace: thingsboard
  # labels:
    # app.kubernetes.io/name: thingsboard
# spec:
  # type: ClusterIP
  # selector:
    # app.kubernetes.io/name: thingsboard
  # ports:
    # - name: http
      # port: 8080
      # targetPort: 8080
    # - name: rpc
      # port: 7070
      # targetPort: 7070
# ---
# k3s klipper-lb (svclb) does not reliably handle a single LoadBalancer
# Service with mixed TCP and UDP ports, so TCP (MQTT) and UDP (CoAP) are
# exposed via separate Services.
#
# Disabled: with hostNetwork enabled on the pod the transport ports are
# already bound directly on the node, so LoadBalancers are not needed on a
# single-node cluster.
#apiVersion: v1
#kind: Service
#metadata:
#  name: thingsboard-mqtt
#  namespace: thingsboard
#  labels:
#    app.kubernetes.io/name: thingsboard
#spec:
#  type: LoadBalancer
#  selector:
#    app.kubernetes.io/name: thingsboard
#  ports:
#    - name: mqtt
#      port: 1883
#      targetPort: 1883
#    - name: mqtt-tls
#      port: 8883
#      targetPort: 8883
#---
#apiVersion: v1
#kind: Service
#metadata:
#  name: thingsboard-coap
#  namespace: thingsboard
#  labels:
#    app.kubernetes.io/name: thingsboard
#spec:
#  type: LoadBalancer
#  selector:
#    app.kubernetes.io/name: thingsboard
#  ports:
#    # ThingsBoard CoAP transport listens on the UDP range 5683-5688
#    - name: coap
#      port: 5683
#      targetPort: 5683
#      protocol: UDP
#    - name: coap-5684
#      port: 5684
#      targetPort: 5684
#      protocol: UDP
#    - name: coap-5685
#      port: 5685
#      targetPort: 5685
#      protocol: UDP
#    - name: coap-5686
#      port: 5686
#      targetPort: 5686
#      protocol: UDP
#    - name: coap-5687
#      port: 5687
#      targetPort: 5687
#      protocol: UDP
#    - name: coap-5688
#      port: 5688
#      targetPort: 5688
#      protocol: UDP
