apiVersion: vault.vault.upbound.io/v1alpha1
kind: Mount
metadata:
  name: algovectra-ssh
  namespace: crossplane-system
spec:
  deletionPolicy: Delete
  forProvider:
    path: algovectra-ssh
    type: ssh
    description: "SSH Secret Engine for Algovectra managed by Crossplane"
  providerConfigRef:
    name: bao-hnatekmar-xyz
